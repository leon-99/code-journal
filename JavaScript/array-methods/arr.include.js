/* Array include method

    include will find an item in an array and return true if it's there.

    Parameters:
        callback:
            the callback function
            
            fromIndex:
                the start position
*/

const arr = ['apple', 'orange']
console.log(arr.includes('apple')) // true