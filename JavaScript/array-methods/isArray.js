/* Array.isArray method

    in JavaScript, arrays are just objects, so if we use typeof opreater on an array it wll return Object.
    so the way to check if array, use Array.isArray() method.
*/

let arr = [1, 2, 3];
console.log(Array.isArray(arr)) // true